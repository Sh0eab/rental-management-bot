# This file contains the credentials for the voice & chat platforms
# which your bot is using for local development.
# https://rasa.com/docs/rasa/messaging-and-voice-channels

# REST API channel for local development
rest:
  # Enable REST API - no additional configuration needed

# Socket.IO channel for real-time communication
socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true

# Local development - no Rasa X needed
# rasa:
#   url: http://localhost:5002
